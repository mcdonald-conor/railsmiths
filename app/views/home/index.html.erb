<div class="min-h-screen bg-gradient-to-b from-gray-900 to-gray-800">
  <!-- Navigation -->
  <nav class="px-4 py-6 md:py-8">
    <div class="container mx-auto">
      <div class="text-white font-bold text-2xl">&#128736; Railsmiths</div>
    </div>
  </nav>

  <!-- Hero Section -->
  <div class="container mx-auto px-4 pt-8 pb-20 md:pt-16">
    <div class="max-w-4xl mx-auto">
      <!-- Main Hero -->
      <div class="text-center">
        <h1 class="text-4xl md:text-6xl font-bold text-white mb-6 leading-tight">
          Stop struggling with
          <span class="bg-gradient-to-r from-blue-400 to-blue-600 text-transparent bg-clip-text">
            mobile development
          </span>
        </h1>

        <p class="text-lg md:text-xl text-gray-300 mb-12 max-w-2xl mx-auto">
          Get pre-configured Turbo Native, authentication, push notifications, and offline sync so you can launch a full-featured mobile app without touching Swift or Kotlin.
        </p>

        <!-- Signup Form -->
        <div class="max-w-md mx-auto mb-12">
          <%= form_with model: @signup,
              class: "space-y-4",
              data: {
                controller: "signup confetti",
                action: "turbo:submit-end->confetti#fire"
              } do |f| %>
            <div id="signup-form" class="flex flex-col sm:flex-row gap-3">
              <div class="flex-1">
                <%= f.email_field :email,
                    class: "w-full px-4 py-3 rounded-lg bg-gray-800/50 border border-gray-700 text-white placeholder-gray-400 focus:ring-2 focus:ring-blue-500 focus:border-transparent",
                    placeholder: "Enter your email",
                    required: true %>
              </div>
              <%= f.submit "Join the Waitlist",
                  class: "w-full sm:w-auto px-8 py-3 bg-blue-600 hover:bg-blue-700 text-white font-semibold rounded-lg transition-colors duration-200",
                  data: { confetti_target: "button" } %>
            </div>

            <%= turbo_frame_tag "flash" do %>
              <% if flash[:notice] %>
                <div class="mt-3 text-sm text-green-400 animate-fade-in"><%= flash[:notice] %></div>
              <% elsif flash[:alert] %>
                <div class="mt-3 text-sm text-red-400 animate-fade-in"><%= flash[:alert] %></div>
              <% end %>
            <% end %>
          <% end %>
        </div>

        <p class="text-blue-400 font-semibold mb-16">🔥 Sign up now and get an exclusive early-bird discount!</p>

        <!-- Feature Grid -->
        <div class="grid grid-cols-1 md:grid-cols-2 gap-6 md:gap-8 mb-20">
          <div class="group p-6 bg-gray-800/50 rounded-xl border border-gray-700/50 hover:border-blue-500/50 transition-all duration-300 text-left">
            <div class="flex items-start gap-4">
              <div class="text-2xl">⚡️</div>
              <div>
                <h3 class="text-xl font-semibold text-white mb-2">Turbo Native Pre-configured</h3>
                <p class="text-gray-400">Get a hybrid mobile app that feels native, right out of the box.</p>
              </div>
            </div>
          </div>

          <div class="group p-6 bg-gray-800/50 rounded-xl border border-gray-700/50 hover:border-blue-500/50 transition-all duration-300 text-left">
            <div class="flex items-start gap-4">
              <div class="text-2xl">🔐</div>
              <div>
                <h3 class="text-xl font-semibold text-white mb-2">Authentication & Push Notifications</h3>
                <p class="text-gray-400">Devise, OneSignal/Firebase ready for immediate use.</p>
              </div>
            </div>
          </div>

          <div class="group p-6 bg-gray-800/50 rounded-xl border border-gray-700/50 hover:border-blue-500/50 transition-all duration-300 text-left">
            <div class="flex items-start gap-4">
              <div class="text-2xl">📱</div>
              <div>
                <h3 class="text-xl font-semibold text-white mb-2">App Store & Play Store Ready</h3>
                <p class="text-gray-400">Deploy directly to both stores without writing native code.</p>
              </div>
            </div>
          </div>

          <div class="group p-6 bg-gray-800/50 rounded-xl border border-gray-700/50 hover:border-blue-500/50 transition-all duration-300 text-left">
            <div class="flex items-start gap-4">
              <div class="text-2xl">🔄</div>
              <div>
                <h3 class="text-xl font-semibold text-white mb-2">Background Sync & Offline Mode</h3>
                <p class="text-gray-400">Works seamlessly even with spotty connections.</p>
              </div>
            </div>
          </div>
        </div>

        <!-- How It Works -->
        <div class="bg-gray-800/30 rounded-2xl p-8 md:p-12">
          <h2 class="text-3xl md:text-4xl font-bold text-white mb-8">How It Works</h2>

          <div class="grid grid-cols-1 md:grid-cols-3 gap-8">
            <div class="text-center">
              <div class="text-3xl mb-4">1️⃣</div>
              <h3 class="text-xl font-semibold text-white mb-2">Clone Railsmiths Mobile</h3>
              <p class="text-gray-400">Get started in minutes</p>
            </div>

            <div class="text-center">
              <div class="text-3xl mb-4">2️⃣</div>
              <h3 class="text-xl font-semibold text-white mb-2">Customize Your App</h3>
              <p class="text-gray-400">Add your features using Rails & Hotwire</p>
            </div>

            <div class="text-center">
              <div class="text-3xl mb-4">3️⃣</div>
              <h3 class="text-xl font-semibold text-white mb-2">Deploy to App Stores</h3>
              <p class="text-gray-400">Full mobile experience, no Swift/Kotlin required</p>
            </div>
          </div>

          <div class="mt-12">
            <p class="text-lg text-blue-400 mb-6">📩 Want early access? Join the waitlist!</p>
            <%= link_to "#signup-form",
                class: "inline-block px-8 py-3 bg-blue-600 hover:bg-blue-700 text-white font-semibold rounded-lg transition-colors duration-200",
                data: {
                  controller: "scroll",
                  action: "click->scroll#scrollTo",
                  scroll_offset_value: "0"
                } do %>
              Get Early Access
            <% end %>
          </div>
        </div>
      </div>
    </div>
  </div>
</div>
