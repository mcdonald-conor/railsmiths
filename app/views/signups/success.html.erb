<div class="min-h-screen bg-gradient-to-b from-gray-900 to-gray-800 flex items-center justify-center"
     data-controller="confetti"
     data-action="turbo:load@window->confetti#fire">
  <div class="max-w-2xl mx-auto text-center px-4">
    <div class="inline-flex items-center justify-center w-20 h-20 rounded-full bg-green-900/30 mb-8">
      <svg class="w-10 h-10 text-green-400" fill="none" stroke="currentColor" viewBox="0 0 24 24">
        <path class="checkmark"
              stroke-linecap="round"
              stroke-linejoin="round"
              stroke-width="2"
              d="M5 13l4 4L19 7">
        </path>
      </svg>
    </div>

    <h1 class="text-4xl md:text-5xl font-bold text-white mb-6">You're on the list! 🎉</h1>

    <p class="text-xl text-gray-300 mb-12">
      Thanks for signing up! We've sent you a confirmation email.<br>
      You'll be among the first to know when Railsmiths Mobile launches.
    </p>

    <%= link_to "Back to Home", root_path,
        class: "inline-block px-8 py-3 bg-blue-600 hover:bg-blue-700 text-white font-semibold rounded-lg transition-colors duration-200" %>
  </div>
</div>

<style>
  .checkmark {
    stroke-dasharray: 24;
    stroke-dashoffset: 24;
    animation: draw 1s cubic-bezier(0.65, 0, 0.45, 1) forwards;
    animation-delay: 0.5s;
  }

  @keyframes draw {
    from {
      stroke-dashoffset: 24;
    }
    to {
      stroke-dashoffset: 0;
    }
  }
</style>
